apiVersion: v1
kind: Service
metadata:
  name: vikunja
  labels:
    app: vikunja
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  selector:
    app: vikunja
  ports:
    - name: web
      port: 80
      targetPort: web
    - name: api
      port: 3456
      targetPort: http
  type: ClusterIP

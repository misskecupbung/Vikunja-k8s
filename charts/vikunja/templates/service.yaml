apiVersion: v1
kind: Service
metadata:
  name: vikunja
  labels:
    app: vikunja
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  selector:
    app: vikunja
  ports:
    # Frontend web UI
    - name: web
      port: 80
      targetPort: web
    # Vikunja API
    - name: api
      port: 3456
      targetPort: http
  type: ClusterIP
